# CLAUDE.md

Guidance for Claude Code (claude.ai/code) in this repository.

Everything lives in `AGENTS.md`, which indexes `docs/`. Keep it that way — one
set of instructions, shared by every agent, rather than a Claude-specific copy
that drifts.

@AGENTS.md
